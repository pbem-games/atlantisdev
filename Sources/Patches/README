There is no gaurentee that any given patch will work
when applied against any given version of the code.

Odds are however, that if it was a patch of general interest
that it has been added to the current codebase.